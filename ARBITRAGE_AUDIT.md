# 套利监控审计与实施记录

更新日期：2026-09-09。用户约束：总资金约 500 美元、本机运行，无专用节点预算。

## 自动链路的当前实现

2026-09-09 已将仅轮询历史机会的初版替换为自动发现管道：

1. 独立调度每 30 秒启动一批发现任务，一次选择 12 个最久未检查的代币。当前可覆盖 559 个非主流 EVM 代币；不是所有代币每 30 秒刷新一次。
2. 代币库与新桥流水提供线索，通过官方合约映射查两端行情。仅当身份、流动性与展示价差条件满足时才进入完整询价队列。展示层门槛为 0.5% 价差、最低 $5,000 流动性；它们不是执行条件。
3. 新桥扫描产生的候选直接进入同一队列；无须用户在页面选择路线。
4. 新路线首次试算 $100，后续发现时轮换 $50 / $200 / $25；有五分钟冷却与队列去重。
5. 四段报价为正后自动查询安全数据，并在预计搬运时间之后、至少一分钟后按原金额独立复测。复测必须关联真实的前次样本，不能由客户端传入计数伪造。
6. 到期的复测会标记过期；负向复测标记信号未重现。两次正向且安全数据通过，也只进入待钱包模拟状态，不发送交易。

网页展示覆盖数量、已轮查数量、最近发现结果、排队数、当前询价步骤以及失败原因。停止开关会清空待处理队列，已在途的只读请求仍会完成并留存样本。

新增验证：test-auto-discovery.js 验证公平轮查、无需历史机会的新发现、身份门槛和缺价行为；test-auto-pipeline.js 验证自动入队、延迟复测、重复证据来源、冷却、停止与未知安全数据。两组已通过。另新增 test-provider-backoff.js，验证 Retry-After、等待期间抑制请求、自动恢复和一小时限流提示解析。

真实运行验收：独立自动发现已经轮查 132 个代币，并在没有手动选择路线的情况下发现 GMX，完成四段报价后得到约 -9.819 美元报价净额，因此拒绝。原始自动样本保存在 data/paper-lab.json，source 为 automatic_discovery。这验证了自动链路运作，不构成盈利证明。

限流处理：报价接口返回 429 后，按 Retry-After 或接口指定时间暂停该服务的后续请求；退避时间持久化到实验室状态，重启后仍尊重等待。发现层可继续轮查展示行情，过期验证任务会被清理。

## 当前结论

系统能够发现值得研究的价差，但原实现没有证明可以按给定金额完成套利。此次已实现独立的四段报价实验室、修复数个数据与证据错误，并在真实 API 上验证了历史候选。没有签名、发送交易或发生已实现利润；不能把此次工作称作“实盘套利成功”。

VITA 曾出现约 100 美元规模的正向报价净额 +4.078 美元，但后续四个金额样本受网络错误阻断；改用直连后，同样约 100 美元的完整复测得到 -7.764 美元。正向信号未得到重复验证。合约安全检查为 unknown，不能解读为通过。

## 原系统为什么难以产出

| 环节 | 代码/数据证据 | 影响 |
|---|---|---|
| 发现 | 941 个代币，275 个有 checkedAt；默认每轮选 12 个，每 5 分钟扫描一次 | 不是全市场连续报价；优先级选择可能长期集中在少数代币。12/941 的吞吐不能保证覆盖短时机会 |
| 扫描 | 最近几次日志明确跳过 priceCheck；Range 缺少 Key；其余桥能返回数据 | 桥消息更新不等于完成套利检查。日志没有足够上下文判断当时为何显式跳过，不能归因于某个确定的操作 |
| 行情 | DexScreener 返回的是池子展示价；旧 tokens 缓存 5 分钟 | 展示价差不能代替指定金额的成交数量；流动性 TVL 也不能代替集中流动性池的实际曲线 |
| 地址 | 行情、入库、解析等路径对所有地址做 toLowerCase | 会破坏 Solana 等区分大小写的标识。历史已丢失的大小写不能逆推出原始地址 |
| 配对 | 批量过滤只留 baseToken；指定池缓存未含目标代币 | quoteToken 侧漏价，同一个池子的两侧查询可能混用价格 |
| 询价 | 搬运数量固定按 18 decimals，目标通道失败后用稳定币通道或固定费用兜底 | 小数位错误；“桥支持稳定币”被错误用于推断“桥支持这枚币” |
| 实时判断 | /api/opportunity/live 用历史快照填补失败的实时请求，并可能返回 ACTIVE | 查询失败可能被展示成机会仍然成立 |
| 评分 | 毛价差、TVL、链名、成交量启发式加分 | 高分不代表净利润、可桥接或可成交；把极端价差直接描述为“证实假币”也缺乏充分证据 |
| 时效 | 更新已存在机会时没有同步更新 ts | 首次发现时间与最新观测时间混淆，无法从旧 ts 单独判断报价新旧 |
| 钱包/账本 | 钱包闭环依赖桥流特征；盈亏为人工记账 | 不是链上逐笔 swap 与回款的会计核验；不能证明所跟踪钱包或本项目已盈利 |

桥适配器、入库、候选选择、解析、裁决、行情、报价、评分、通知、账本和前端连接均已检查。此次没有逐条向所有链 RPC 验证每一笔历史桥事件，也没有完成全部协议的原生桥审计。

## 已实施的变化

- 新增 lib/amounts.js：十进制字符串与 BigInt 基础单位转换；只规范化十六进制地址大小写。
- 修复 prices/store/db/resolver/engine/wallet-scorer 中相关地址路径，保留非 EVM 标识。
- 修复 quoteToken 批量遗漏、无关合约错误匹配、按池缓存的方向混淆；精确指定的池失败时不悄悄换池；force 支持重新请求。
- 展示行情缓存缩为 tokens 30 秒、pair 10 秒；成交验证独立请求，不依赖这些缓存。
- 重写 crosschain-quoter：查真实 decimals；取消稳定币通道与经验费用兜底。这个兼容接口仍只是单段报价，不是完整套利证明。
- 修复 live 接口：实时失败不拿快照补价，估算明确标注不可执行，不再用正的展示价差宣布 ACTIVE。
- 机会去重加入两端合约；更新机会时保留 firstSeen，并更新观测 ts。
- 新增 lib/lifi-readonly.js 与 lib/cycle-validator.js：明确资产、链、数量与精度，依次检查买入、代币跨链/组合路由、卖出、稳定币回源链。
- 各段使用上一段 toAmountMin 作为下一段输入；最后比较源链同一结算资产余额，单列 gas 和未含在输出内的费用，避免重复扣除已含费用。
- 新增本机实验室 /lab.html、三个 /api/lab/* 接口、独立样本文件、自动观察与 JSON 导出。实验室不保存交易 calldata、不连接钱包、不签名、不广播交易。
- 本次运行仅绑定 127.0.0.1；关闭本次进程的外部通知发送。LI.FI 使用直连，其他数据源原设置保留。

## 实测证据与解释

原始样本：data/cycle-audit.json、data/vita-validation.json、data/vita-direct.json。文件仅包含研究所需的公开合约、报价及检查结果，没有私钥或 API Key。

首轮对可支持的 9 条历史 EVM 候选做了约 100 美元规模验证：

- NPC Ethereum → Base：四段完成，报价净额 -4.949 美元；询价窗口也超过 60 秒。
- XAN BSC → Ethereum：两段完成，目标链卖出没有聚合器路由。
- XAN Ethereum → BSC：买入可报价，跨链段没有聚合器路由。
- VITA Base → Ethereum：四段完成，源链结算币起始约 100 美元，返回约 105.749 美元；gas 0.6712 美元，另扣 1 美元额外费用缓冲，报价净额 +4.078 美元。
- GIV、$SKOL、PUFFER、RIVER、FOLKS：不同阶段接口超时，无法得出完整净收益结论。
- VITA 25/50/100/200 美元复测：均在部分阶段遇到网络错误；安全检查 unknown。
- VITA 100 美元直连复测：四段完成，报价净额 -7.764 美元。

VITA 的跨链段包含 swap + cross，不能称为已证明的原币一比一搬运。缺少 LI.FI 路由只表示本次该聚合器没有报价，并不能证明所有原生桥都不可用。报价最小输出也不是对未来四笔独立交易的组合保证。

## 500 美元条件下的实施方案

1. **先淘汰假机会。** 继续使用原雷达发现候选，以确切链和合约送入实验室；不要因为想要更多告警而降低身份与费用检查。
2. **以小金额形成证据。** 自动发现每 30 秒调度一批代币；新路线自动进入完整验证，按 100/50/200/25 美元轮换试算。单笔试算最多 450 美元，预留 50 美元用于预算检查。此为研究配置，没有真实资金被分配。
3. **复核持续性和原生通道。** 只有多次报价仍为正、经过至少一个预计搬运时间后仍成立，才值得进一步分析。逐段确认聚合路由是否引入了卖出再买入；若是，单独核对该代币的官方桥映射与实际费用。
4. **用户实盘前的缺口。** 需要明确资金所在链和公开钱包地址，核验合约身份、安全性、两端 Gas 余额、授权、实际路由容量和退出成本，并针对该钱包做交易模拟。此工作目前未完成；不要把通用地址询价当成钱包可执行检查。
5. **真实验收标准。** 用户自行签名的交易完成后，用两端交易回执和最终回款核算：源链同一结算资产净增，扣除所有 gas/桥费/授权费/兑换费用，不残留未计价仓位。只有这一步能够证明实际盈利。

低资金不应先购置节点或部署高频设施。现在观察到的主要问题是缺少完整成交证据、路由覆盖与报价稳定性；增加基础设施不能修复错误的利润计算。跨链串行交易承担价格变化风险，本机监控不能保证每次找到可盈利路线。

## 本机使用

当前服务入口：http://127.0.0.1:8848/lab.html

手动启动这次研究配置：

```bash
node tools/start-local-paper.js
```

它启动本机后台服务，日志为 data/local-paper-server.log，PID 为 data/local-paper-server.pid。自动观察开关在实验室页面；停止自动观察不影响原雷达。关机、休眠或结束服务会停止采样；没有安装开机启动服务。

复跑历史候选（只读主库，写独立证据文件）：

```bash
RADAR_LIFI_DIRECT=1 node tools/audit-opportunities.js 10 100
```

## 验证与剩余限制

通过：test-cycle-validator（19 项）、test-price-integrity（8 场景）、test-live-evidence、test-security-unknown、test-best-verdict（12 项）、test-adjudicate（5 项）、Vite 生产构建与 git diff --check。

全量 TypeScript 检查未通过，报告了未使用导入、既有 LiveQuoteData 字段、NodeJS 命名空间和 CSS 类型声明等问题；此次没有将这些无关类型问题扩展为整体前端重构。

现有 security-checker.js 与 test-security-unknown.js 在本次工作开始前已有未提交改动；保留了这些改动，并运行了该安全回归测试。

本次验证器只覆盖已配置的七条 EVM 链和 LI.FI 路由；它不支持自主实盘下单、没有进行钱包级链上模拟、不负责原生桥合约发现，也不能恢复历史已破坏的 Solana 地址。1 美元额外缓冲只是显式假设，不是已核实的授权费与失败成本。

## 官方接口依据

- [DEX Screener API](https://docs.dexscreener.com/api/reference)：池子价格、流动性和成交量数据。
- [LI.FI Quote](https://docs.li.fi/api-reference/get-a-quote-for-a-token-transfer)：action、estimate、toAmountMin、gasCosts、feeCosts 与 includedSteps。
- [LI.FI 多步骤滑点说明](https://help.li.fi/hc/en-us/articles/12111462474139-How-does-slippage-and-toMinAmount-works-in-routes-with-multistep-transaction)：应跟踪输出最小值；多步骤滑点不能简单当成整个闭环统一容差。
- [LI.FI 响应解释](https://help.li.fi/hc/en-us/articles/16509431041307-How-do-I-use-the-API-endpoints-and-interprete-the-responses)：路线可能结合兑换与跨链步骤，市场变化可能导致交易失败。
