# 刷新与外部请求协调说明

更新时间：2026-09-09。

## 诊断结论

“页面刷新”与“外部采集”不是同一件事。此前各后台模块虽有各自的 busy 标志，但未共享总并发、服务商退避和报价用量预算。详情弹窗默认每 10 秒实时验价，设置的桥扫描开关不控制它，也不控制自动监控。

| 入口 | 实际行为 | 是否使用外部额度 |
| --- | --- | --- |
| 主页 SSE、状态刷新 | 读取本服务已保存的数据 | 否 |
| 桥流水列表刷新 | 读取本地流水 | 否 |
| “立即扫描”或拉取桥流水 | 发起一次后台桥扫描 | 是 |
| 设置中的自动扫描 | 定时扫描桥流水，并按配置比价 | 是 |
| 自动监控 | 每 30 秒发现候选，排队验证四段报价 | 是 |
| 监控页每 5 秒更新 | 读取本地监控状态和历史报告 | 否 |
| 矩阵展开、详情验价 | 请求行情、安全数据和代币跨链报价 | 是 |

## 实施后的保护

- 所有通过 lib/net.js 发出的 API / RPC 请求共享最多 4 个并发、100 个排队位置；排队超过 15 秒则明确返回失败，不持续积压。
- 按主机约束请求频率和并发，GeckoTerminal / CoinGecko 采用每次至少间隔 2.1 秒、单并发；LI.FI 至少间隔 1 秒，其余默认至少间隔 0.5 秒。它们是本机保护参数，不代表服务商承诺的配额。
- 相同的在途 GET 和明确列入允许范围的只读 JSON-RPC 请求合并；请求头、网络配置和请求体不同的调用不混用结果。交易写请求不合并、不自动重试。
- HTTP 429 和已识别的 JSON-RPC 限流响应在整个服务内共享退避。优先采用 Retry-After、ratelimit-reset 和服务商消息；使用其中更晚的恢复时间。停止批量行情的限流重试与单地址降级风暴。
- LI.FI 报价采用本机滚动两小时动态预算，记录在 data/request-budget.json。若配置了有效 API Key，按官方 100 RPM（100 × 120 = 12,000 次/2小时）上限执行；未配置 Key 时按公共安全预算 60 次/2小时执行。重启保留用量、暂停状态和已知服务商冷却时间。额度不足 4 次时不启动新的完整验证。
- 新候选完整验证最多每 8 分钟启动一次，已获得正向报价的到期复测优先；最终仍受剩余额度和服务商退避约束。发现候选可以继续，排队过期的证据不会拿来充当新报价。
- 单代币行情复用现有 30 秒发现缓存；各页面实时验价共享在途任务与 10 秒展示缓存，快速强制刷新至少复用最近 2 秒的结果。结果保留原报价时间，不重新盖“新报价”时间戳。
- 详情定时验价默认关闭；手动开启后每 30 秒检查一次，隐藏页面或已有请求未完成时不重叠调用。主页与监控的状态读取合并在途请求；隐藏页面暂停轮询。
- 只保留一次扫描完成通知，且在释放扫描状态后发送。配置局部保存保留未修改的扫描参数。
- 稳定币脱锚专项雷达 (Depeg Watch) 采用 DeFiLlama 批量公开聚合端点（一次 HTTP 请求覆盖全链原生稳定币，无速率限制），结合 DexScreener 流动性过滤，不消耗 LI.FI 报价配额。

## 开关含义

设置页“自动扫描”只控制定时桥扫描；监控页“自动观察”只控制机会发现与四段验证。停止监控后，已发出的请求允许结束，后续报价段和复核不再启动。

设置页与监控页都有“请求与后台任务”面板。“暂停全部外部请求”控制本服务共用的网络出口：排队与后续请求会被拦截，已经发出的请求允许结束；页面仍可读取本地结果。恢复时保留各后台开关原先的偏好，不擅自启用已关闭的任务。

本次部署保留了用户保存的桥扫描关闭、自动监控关闭状态。

## 适用范围

保护范围是同一 Node 服务实例内的页面、用户与后台模块。供应商可能按账号、API key、IP 或端点共享额度；其他程序和其他服务实例的请求无法由本项目统计或约束。多实例部署需要额外共享额度存储。

LI.FI 两份官方说明存在数值差异：
- https://docs.li.fi/api-reference/rate-limits
- https://help.li.fi/hc/en-us/articles/12111455848859-What-is-the-LI-FI-API-rate-limit

因此使用保守的本机预算，并遵守实际响应的退避信息。界面显示的是本服务记录的用量，不冒充账户可用配额。没有引入轮换密钥或 IP 绕过限制的行为。

## 验证

模拟测试：test-request-governor、test-request-controls、test-monitor-stop，以及已有自动发现、自动复测、报价限流、价格完整性和四段验证回归测试。

浏览器验证：桌面与手机无溢出；设置与监控均显示共享控制面板；并行读取监控状态只发出一份在途本地请求；隐藏标签页暂停轮询；总暂停跨页面一致；打开与刷新页面没有新增服务商请求。

前端 Vite 构建通过。未进行真实交易，也没有为测试发起外部报价。
